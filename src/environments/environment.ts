export const environment = {

    production: false,
  apiUrl: 'http://localhost:3000/auth',  // Your backend API URL
  authTokenKey: 'auth_token'  // Key to store JWT in localStorage/sessionStorage
};
